<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.KnowMovies" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="background">@color/surface</item>
        <item name="android:statusBarColor">@color/surface</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorAccent">@color/search_light</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="itemBackground">@color/search_dark</item>
    </style>
</resources>